/* StreakGrid deployment config.
 * googleClientId: OAuth 2.0 Web Client ID from Google Cloud Console
 * (see README → "Enable Google Drive sync").
 * Users can also paste a client ID in Settings, which overrides this.
 * The app works fully local-only when this is empty.
 */
window.SG_CONFIG = {
  googleClientId: ""
};
